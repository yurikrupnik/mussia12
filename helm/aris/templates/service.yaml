apiVersion: v1
kind: Service
metadata:
  name: {{ include "aris.fullname" . }}
  labels:
    {{- include "aris.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aris.selectorLabels" . | nindent 4 }}
