apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aris.fullname" . }}-test-connection"
  labels:
    {{- include "aris.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aris.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
