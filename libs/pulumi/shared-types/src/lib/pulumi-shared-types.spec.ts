import { pulumiSharedTypes } from './pulumi-shared-types';

describe('pulumiSharedTypes', () => {
  it('should work', () => {
    expect(pulumiSharedTypes()).toEqual('pulumi-shared-types');
  });
});
